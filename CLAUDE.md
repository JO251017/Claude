# 저장소 메모

이 저장소는 여러 개인 프로젝트를 번호가 붙은 폴더 단위로 담는다 (`N. 이름` 형식, 예: `1. Informacion`, `2. 다음분야`).

**규칙: 각 프로젝트 폴더는 자기만의 `CLAUDE.md`를 갖고 그 안에서 독립적으로 관리된다.**
- 특정 프로젝트 관련 요청을 받으면 그 프로젝트 폴더의 `CLAUDE.md`를 먼저 읽고 그 안의 절차를 따른다.
- 새 프로젝트 폴더를 만들 때는 반드시 그 폴더 안에 `CLAUDE.md`를 함께 만든다 (전역 규칙은 여기, 프로젝트별 세부 규칙은 각 폴더 안에).
- 이 루트 파일은 프로젝트 목록과 공통 규칙만 관리하고, 프로젝트별 세부 내용(포맷 규격, 제작 절차, 입력 단위가 "주제"인지 다른 무엇인지 등)은 절대 여기 적지 않는다.

## 요청 라우팅 규칙
- 사용자 메시지가 `프로젝트명: 내용` 형식으로 시작하면(예: `Informacion: 아이스크림 콘의 역사`), 아래 목록에서 그 이름과 일치하는 폴더를 찾아 그 폴더의 `CLAUDE.md` 절차를 그대로 따른다.
- 프리픽스가 없어도 내용상 어느 프로젝트인지 명백하면 그 프로젝트로 바로 진행한다.
- 프로젝트가 여러 개 있고 어느 쪽인지 애매하면, 추측하지 말고 사용자에게 먼저 확인한다.
- 이 프리픽스 규칙은 프로젝트 수가 몇 개든(2개든 100개든) 그대로 유지된다 — 프로젝트별로 별도 트리거 문구를 만들지 않는다.

## 현재 프로젝트 목록
- `1. Informacion/` — 카드 스토리텔링 콘텐츠 프로젝트
